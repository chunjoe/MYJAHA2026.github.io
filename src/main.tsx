import React from 'react'
import ReactDOM from 'react-dom/client'
import PaperIntro from './PaperIntro'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PaperIntro />
  </React.StrictMode>,
)
